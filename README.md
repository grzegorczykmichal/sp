Run `npm i` followed by `npm t`.

See `/src/responsify` folder to find the solution.

`src/add`, `/src/getContainerWidth`, `/src/maxValueKey` contain example usage of the `responsify` function.

Have fun.
